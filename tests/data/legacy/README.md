These files are tracked legacy reference fixtures used by the test suite.

- `example/` contains the specific legacy example curves used by the DOS and
  Lindhard regression tests.
- `test/` contains the specific legacy shell reference outputs used by the
  shell-matrix parity tests.

They were copied from a local `libtetra_original/` checkout so CI does not need
that ignored tree to validate `bztetra`.
